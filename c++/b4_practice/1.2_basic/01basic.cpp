#include<bits/stdc++.h> // c++ 코드의 기본 형태
 
using namespace std; // 네임스페이스 -> 많은 라이브러리를 불러서 사용할때 변수명 중복 발생 가능 / 이를 방지하려고 변수명에 범위를 걸어놓는 것 
string a;

int main(){
	cin >> a; // namespace덕에 std::cin으로 표기할 필요 없음 
	cout << a << "\n"; // namespace덕에 std::cout로 표기할 필요 없음
	return 0;
}

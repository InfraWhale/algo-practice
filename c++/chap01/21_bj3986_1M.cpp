#include <bits/stdc++.h> // 백준 3986 1M
using namespace std;

int n, cnt;

string temp;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n;

    for(int i=0; i<n; i++) {
        int A, B; //0: 닫힘, 1: 열림
        cin >> temp;
    }

    cout << cnt << "\n";

    return 0;
}